[project]
name = "expect-ime"
version = "0.1.0"
description = "Mozc-inspired Japanese IME typo detection prototype"
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
